# Use ELECTRON_MIRROR when GitHub releases are slow (see scripts/ensure-electron.mjs).
# npm config key for @electron/get mirror (also set in task electron:* env):
electron_mirror=https://npmmirror.com/mirrors/electron/
